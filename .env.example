# Base de datos Postgres
DATABASE_URL="postgresql://usuario:password@localhost:5432/fondos_a_rendir?schema=public"

# Firma de las cookies de sesión (usa un valor largo y aleatorio en producción)
SESSION_SECRET="cambia-este-valor-por-uno-aleatorio-y-largo"

# Envío de correo (PIN de acceso y notificaciones). Sin esta variable, el
# PIN se muestra en pantalla y los correos quedan solo en el log del servidor.
RESEND_API_KEY=""
EMAIL_FROM="Fondos a Rendir <onboarding@resend.dev>"

# Roles fijos por correo
ADMIN_EMAIL="jorge.rojas@copayapunos.cl"
APPROVER_EMAIL="williams.arce@copayapunos.cl"

# Correos que reciben el certificado de pago cuando el administrador lo envía
# (separados por coma)
PAYMENT_NOTICE_EMAILS="contabilidad@copayapunos.cl,andres.rojas@copayapunos.cl"

# URL pública de la app (para armar enlaces en los correos). Si se omite,
# se infiere del encabezado Host de cada request.
APP_URL=""

# Almacenamiento de fotos/documentos/certificados adjuntos.
# Si configuras las 4 variables R2_*, se guardan en Cloudflare R2 (recomendado
# para producción: sobrevive aunque se reinicie o cambie el hosting de la app).
# Si las dejas vacías, se guardan en disco local (ATTACHMENTS_DIR, por
# defecto ./storage/attachments) — solo apto para desarrollo o pruebas.
R2_ACCOUNT_ID=""
R2_ACCESS_KEY_ID=""
R2_SECRET_ACCESS_KEY=""
R2_BUCKET_NAME=""
# Endpoint S3 exacto (opcional). Cópialo tal cual desde Cloudflare: tu bucket
# → Settings → "S3 API". Si lo defines, tiene prioridad sobre R2_ACCOUNT_ID
# para armar la URL (útil si tu cuenta usa una jurisdicción distinta a la
# por defecto, ej. *.eu.r2.cloudflarestorage.com).
R2_ENDPOINT=""
ATTACHMENTS_DIR=""
